# 东北大学教务焕新 0.3.47 代码审查与修复报告

## 审查范围

本次逐项检查了 `manifest.json`、MV3 service worker (`background.js`)、查询台 (`dashboard.js` / `dashboard.html` / `dashboard.css`) 以及现有接口/设计文档。修复遵循“功能零回归”原则：不删除现有查询入口、不缩减接口兜底、不改变既有成绩/GPA/考试/个人课表/全校课表/培养方案/导出功能，只处理确定会导致错误结果、状态串扰、静默缺数据或兼容性下降的问题。

## 已修复问题

### 1. 异步请求竞态导致旧数据覆盖新选择（高）
- 普通数据刷新：快速切换学期或连续刷新时，旧学期请求可能后返回并覆盖当前学期。
- 全校课表：连续查询、切换类型或切换学期时，旧请求可能覆盖新查询。
- 全校课表详情：快速打开不同班级/教师/教室详情时，旧详情可能覆盖新详情。
- 培养方案列表/方案详情：快速刷新或切换方案时，旧结果可能写回当前界面。

处理：给这些共享状态异步流程加入请求代次号，并在每个关键 `await` 后校验，只允许最后一次请求提交状态。

### 2. 全校课表与普通刷新共用 `state.loading`（高）
全校课表查询与成绩/考试/个人课表刷新并行时，一个任务先结束会把另一个任务的加载状态提前清空，造成提前退出加载、显示旧数据或空结果。

处理：全校课表改用其独立的 `allRetrying` 状态，不再写入普通数据的 `state.loading`。

### 3. 全校课表详情分页失败会静默丢课（高）
`loadAllScheduleList()` 原先用 `Promise.allSettled()` 读取后续页，但直接忽略 rejected 页，最终仍会显示“已读取全部分页”。这会产生看似成功、实际缺课的结果。

处理：后续分页限制为每批 5 页；失败页单独重试一次；仍失败则明确抛错，让上层继续现有 KBLX/接口兜底，而不是返回不完整数据。

### 4. 动态 `queryAction` 被中文名称硬编码覆盖（中高）
原系统已经返回课表类型对应的 `queryAction`，但旧代码优先根据“班级/教师/教室”中文名称猜接口，接口映射或名称变化时可能请求错误端点。

处理：服务器返回的 `queryAction` 优先；中文名称只用于旧接口缺少 `queryAction` 时兜底，并保留原来的班级默认端点。

### 5. 缺少 `queryAction` 的合法课表类型被直接过滤（中）
旧代码加载类型时要求 `code && queryAction`，而代码本身已经为班级/教师/教室准备了动作兜底，导致部分旧版/变体接口返回的类型无法使用。

处理：只要求存在 `code`；查询时通过现有 `allScheduleAction()` 兼容缺失动作。

### 6. 全校课表直接返回课程行时无法打开课程详情（中）
直接课程结果使用 `scope="all"` 渲染，但课程索引逻辑只认识个人课表和 `all-detail`，所以行看起来可点击却没有有效 `data-action`。

处理：补齐 `all` scope 的索引、课程集合和详情映射，不改变个人课表/全校详情的原逻辑。

### 7. 培养方案合并索引没有随真实 ID 更新（中）
从原页面得到的 fallback `portal-name:*` 方案与接口真实 `PYFADM` 合并后，对象 ID 会更新，但 `byId/byName` 索引仍指向旧键；后续同一真实 ID、名称略有变化的记录可能再次被追加，形成重复方案。

处理：合并后同步删除旧索引并写入新 ID/名称索引。

### 8. Runtime message 超时计时器未清理（中）
培养方案跨标签页读取使用 `Promise.race()`；消息先返回后，超时 Promise 的计时器仍存活。虽然不会改变已完成 Promise 的结果，但会留下不必要的定时任务，并增加潜在未处理错误/警告。

处理：统一为 `runtimeMessageWithTimeout()`，成功/错误/同步异常均清理 timer，并消费 `chrome.runtime.lastError`；一键打开培养方案也增加 18 秒上限，避免消息通道异常时永久等待。

### 9. 不存在的考试日期会被 JavaScript 自动“纠正”（中）
`new Date(2026, 1, 31)` 会自动变成 3 月日期。旧代码只检查时间戳有限，因此 `2026-02-31` 会被当作有效考试日期并参与排序/倒计时。

处理：创建日期后反校验年、月、日，非法日期返回 `null`。

### 10. 成绩全学期结果与学期代码对应方式脆弱（低）
旧代码用 `results.indexOf(result)` 反查并发结果下标，既多一次线性查找，也依赖对象引用唯一性。

处理：直接使用 `forEach((result, index) => ...)` 对应 `codes[index]`。

### 11. 包内容/文档一致性（低）
- `manifest.json` 为 0.3.46，但 README 写 0.3.45。
- README 含本机绝对路径的 Android 工程/APK 链接，分发后无效。
- 考试功能说明重复一条。
- 压缩包含 `__MACOSX/._*` 元数据。

处理：版本统一为 **0.3.47**，补全 manifest 描述中的培养方案，清理无效本机路径、重复文档和 macOS 归档垃圾文件。

## 保留并确认未削减的功能

- 成绩完整字段、分项成绩、累计 GPA 及原有年级规则。
- 培养方案读取、原系统后台标签页桥接、成绩联动、通识选修归类、PDF 导出。
- 考试解析、排序、倒计时。
- 个人课表列表 + 网格合并、周次筛选、日/周视图、课程详情。
- 全校课表动态类型/独立学期、班级/教师/教室查询、详情网格、分页和延迟重试。
- PNG 课表导出。
- `/http/` + `/https/` WebVPN 入口兜底、`kbapp` + `kbbpapp` 兼容。
- Android `AndroidApi` 网络/保存/打印兼容分支。
- 登录方式和一键打开原系统能力。

## 安全与合理性检查

- 无 `eval()` / `new Function()` / 远程脚本注入。
- 无第三方运行时依赖。
- MV3 权限仅为 `scripting`、`tabs`，host permission 仅限 `https://webvpn.neu.edu.cn/*`；现有权限均有实际用途。
- 动态教务字段在 HTML 渲染路径中继续通过 `escapeHtml()`，未发现新增 XSS 拼接点。
- 插件没有保存账号或密码；继续复用当前浏览器/WebVPN 登录会话。
- 没有为了“重构”改写现有网络层或接口列表，避免破坏学校 WebVPN 的特殊兼容逻辑。

## 验证结果

已执行：

1. `node --check background.js`：通过。
2. `node --check dashboard.js`：通过。
3. `manifest.json` JSON 解析：通过。
4. HTML 解析：无重复 ID；`dashboard.js` / `dashboard.css` 本地资源均存在。
5. CSS (`tinycss2`) 解析：0 个顶层语法错误。
6. `tests/audit_smoke.js`：通过，覆盖日期、HTML 转义、课表节次/单双周、同课多时段保留、GPA 规则、培养方案去重、动态课表接口映射、全校课程详情索引、分页重试、加载状态隔离、旧查询不覆盖新查询。
7. Chromium `--pack-extension`：通过，说明最终目录可被 Chromium 识别并打包为扩展。

## 验证边界

当前隔离环境没有你的东北大学已登录 WebVPN 会话，因此无法对学校线上接口做真实账号的端到端请求。为降低这一风险，本次没有删除或重写任何现有 API 兜底路径，修复集中在请求状态管理、结果完整性和纯解析逻辑；线上接口行为仍建议你加载 0.3.47 后用自己的登录会话做一次成绩、培养方案、个人课表、全校课表四项快速验收。
