# 执掌东大 · UI/UX 规范

这是一款学生每天使用的教务工具，不是宣传页或 SaaS Dashboard。界面优先展示今天、下一节课、下一场考试和真实数据；说明文字只在帮助、异常和设置中出现。

## 信息架构

### 桌面端

```text
固定左侧栏
├── 总览
├── 课表
├── 考试
├── 成绩
├── 全校课表
├── 培养计划
└── 设置

固定顶部工具栏
├── 当前页面
├── 当前学期
├── 数据状态
├── 刷新
└── 原系统
```

### Android / 窄屏

```text
顶部工具栏
├── 页面标题
├── 当前学期
└── 刷新

主内容
└── 列表、时间线、抽屉 / Bottom Sheet

底部导航
├── 首页
├── 课表
├── 成绩
├── 考试
└── 更多
```

Android 的 `更多` 负责低频入口；全校课表、设置和登录入口不会和首页重复铺开。培养计划仍依赖桌面原系统后台会话，因此只在桌面端显示。

## Design tokens

```css
--bg: #F6F7F9;
--surface: #FFFFFF;
--surface-subtle: #FAFBFC;
--surface-hover: #F3F5F8;
--text-primary: #1C1C1E;
--text-secondary: #6B7280;
--text-tertiary: #9AA1AB;
--border: #E5E7EB;
--brand: #315D8F;
--brand-hover: #274C76;
--brand-soft: #EDF3F8;
--success: #2E7D5B;
--warning: #9B6A24;
--danger: #B44C4C;
--radius-sm: 6px;
--radius-md: 8px;
--radius-lg: 12px;
```

颜色只用于导航选中态、主要操作、GPA 和异常状态。默认内容使用白底、浅边框、间距和文字层级区分，不使用大面积渐变或阴影。

## 组件规则

- 普通内容使用列表、表格和分隔线；新增容器前先确认是否可以直接用文字层级解决。
- Badge 只表示状态；课程性质、学分和考核方式使用次级文字，以“必修 · 考试 · 3 学分”呈现。
- 普通圆角只使用 6px / 8px；Dialog、Bottom Sheet 使用 12px。
- 动效只改变背景、边框和透明度，时间 120–180ms；不使用悬浮上移和弹性缩放。
- 字体使用系统字体栈，正文 12–14px，页面标题 21–23px，核心数值只在 GPA、成绩和时间上使用强调色。
- 桌面应用固定高度，侧栏和顶部栏固定，只有主内容滚动。
- 移动端不以横向滚动表格作为主要体验；成绩使用列表，课表使用时间线，详情使用 Bottom Sheet。

## 页面重点

- 总览：日期 / 学周、下一节课、今日时间线、近期考试、最新成绩。
- 课表：日视图默认今天和明天；周表保留完整节次网格；导出、数据源、课程记录放入次级操作。
- 成绩：顶部 GPA，主表只保留课程、学分、成绩、绩点、性质、学期；点击课程成绩打开详情抽屉。
- 考试：下一场考试优先；后续考试使用紧凑列表；已结束考试默认折叠。
- 全校课表：先选学期和查询类型，再进行班级、教师、教室查询；结果直接进入紧凑表格和课表详情。
- 培养计划：进度、课组树和课程清单通过分隔线组织，不使用多层 Card。
- 设置：按账户、课表、更多工具、缓存分组，每个设置是一行或一个小 Section。

## Android E 码通

原生 E 码通 WebView 保留登录、二维码刷新和 Token 行为。默认只显示约 112dp 的校园码入口和二维码缩略图，点击后展开完整页面；收起后不再长期占用首页的一大块空间。

## 本地课表覆盖层

本地安排沿用学校课表的排版语言，但用轻量来源标识区分，避免用户误以为数据已写入教务系统：

- 自定义重复课程显示“自定义”标识，使用浅蓝色边框/底色；一次性日程显示“日程”标识，使用浅青色；停用记录降低文字与边框对比度但仍可在管理页恢复。
- 个人课表首屏提供“添加安排”和“管理自定义安排”，日视图/总览把本地记录放在同一时间线上；没有学周时，一次性日程仍按真实日期显示，重复课程明确提示需要设置第一周周日。
- 编辑器使用课程 / 日程两个明确模式。课程要求学期、周次、星期和节次；日程要求日期，可选全天、起止时间和节次。保存前显示确定冲突或可能冲突，并让用户选择保留现有、同时保留或仅保留新安排。
- “仅保留新安排”只改变本地合并层，管理页单独列出被隐藏的教务排课并提供恢复操作；设置页把“教务缓存”和“自定义课表”分成两个 Section，避免清除操作产生误解。
- 周表网格优先展示有节次的安排；只有日期/星期但没有节次的安排落到网格下方的其他日程区。PNG 延续节次 × 星期布局，CSV 对无法表达的日期日程说明跳过原因，不用虚构学校排课字段。
